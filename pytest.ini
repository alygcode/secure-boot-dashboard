[pytest]
testpaths = tests
markers =
    payload: Payload schema and data contract tests
    kql: KQL query structure and consistency tests
    classification: Device posture classification logic tests
    dcr: DCR transform field mapping tests
    workbook: Azure Workbook template integrity tests
    powershell: PowerShell script structure tests
    crossartifact: Cross-artifact consistency tests
